# This is an example use of annotation for dissaggregate accessibilities
# But this particular case could probably be handled using variable mapping
Description,Target,Expression
auto ownership,auto_ownership,"households.veh"
auto ownership,auto_ownership_clipped,"np.clip(households.veh, 0, 2)"
